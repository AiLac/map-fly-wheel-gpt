# 业务流程图

按发布契约从已验证图生成 Mermaid。每张图只表达一个层级或关系，使用 `flowchart TD`、短标签与显式条件。拆图保留父图和子流程导航；不删分支降低复杂度。

每个节点/边在 graph.json 中可定位；条件来自有证据的表达式。不可将 AST 遍历顺序画成执行顺序。图外说明循环、异常、异步边界与未完成 frontier。

Publisher 生成版本目录中的 flow.md；需要改变图时更新 graph 后发布新版本，不改不可变历史文件。
