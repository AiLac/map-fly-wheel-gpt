# 服务交互与时序

| 调用点 | 调用方 | 目标服务/端点 | 条件 | 时序依据 | 代码证据 |
| --- | --- | --- | --- | --- | --- |

静态已确认 RPC 关系可以列入交互表；没有因果证据时不强排全局时序。异步启动、join、聚合、条件路由和回退分别说明。

确有因果依据且图更清楚时，Agent 可补充 sequenceDiagram；参与者过多按子流程拆分，使用 alt/opt/par 表达可证明的语义。禁止把所有可能分支拼成一次请求的实际调用。
