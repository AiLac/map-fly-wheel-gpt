# `<case-id>`：RPC 独立验证样本

- 类型：positive / negative / held_out
- 版本与来源：`<原始代码/框架测试/程序员已知调用对>`
- 是否参与规则推导：`<是/否>`
- 独立期望依据：`<不能由待验证规则自己生成>`

## 输入

列出 consumer、provider、服务配置与依赖的源引用，保留关键 imports。

## 预期

匹配哪个端点或为什么不得匹配；未知处保持未知。包括 service/schema/operation 与适配的依据。

## 实际观察

实际执行命令/代码走读、产物、差异与证据。未运行填“未验证”，不能填成功。
