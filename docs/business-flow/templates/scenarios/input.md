# `<scenario-id>` 输入与范围

- 标题与业务动作：`<基于用户说明或源证据>`
- run_id / snapshot：`<当前冻结运行>`
- 原始选择器：`<class、URL 或 all 中的具体入口>`
- 已确认入口：`<repo/module/service/FQN/method/HTTP 路由>`
- 确认依据：`<evidence refs>`
- 使用的规则/映射/记忆版本：`<实际读取的版本>`

## 请求与返回

说明请求关键字段、前置校验、结果类型和可证明的业务含义。缺少业务解释时列问题。

## 分析范围

从请求入口到返回的阶段、下游边界、相关异常/回退、异步汇合。接受的外部终点与因代码缺失无法继续的 frontier 分开。

## 排除与决定

记录程序员明确接受的排除及其原始证据。不能为达到 complete 而擅自减少范围。
